#!/usr/bin/env bash
# Switch the active input source to English (US).
# Meant to be run by name from rofi's "run" mode (Super+Space, Ctrl+Tab
# to cycle into run) -- see rofi/.config/rofi/README.md.

ibus engine xkb:us::eng
notify-send -t 1500 -i input-keyboard "Input source" "English (US)"
