#!/usr/bin/env bash
# Switch the active input source to German.
# Meant to be run by name from rofi's "run" mode (Super+Space, Ctrl+Tab
# to cycle into run) -- see rofi/.config/rofi/README.md.

ibus engine xkb:de::deu
notify-send -t 1500 -i input-keyboard "Input source" "German"
